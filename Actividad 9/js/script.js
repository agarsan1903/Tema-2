function calcularFactorial() {
    let numero;
    let resultado=1;

    numero = parseFloat (document.getElementById ("numero").value);

    for ( let iteracion =1; iteracion <=iteracion; iteracion=iteracion + 1){
        resultado = resultado * 1
    }
    document.getElementById ("mensaje").innerHTML = "El factorial es" + resultado 
}